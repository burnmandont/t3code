# Store as /etc/t3-sovereign/pull.env on the second proxy, owned by root and
# mode 600. Targets use the dedicated forced-command keys from the runbook.
T3_SSH_CONFIG=/etc/t3-sovereign/ssh/config
T3_EDGE_SSH=t3-cert-edge
T3_COOLIFY_SSH=t3-cert-coolify
